{
  "name": "memocast",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev wiki --host 0.0.0.0",
    "generate:graph": "node tools/knowledgeGraph.mjs",
    "generate:charts": "node tools/datasetCharts.mjs .",
    "audit:citations": "node tools/citationAudit.mjs .",
    "build": "npm run generate:graph && npm run generate:charts && npm run audit:citations && vitepress build wiki",
    "preview": "vitepress preview wiki --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "latest",
    "vitepress": "latest",
    "vitest": "latest",
    "vue": "latest"
  }
}
